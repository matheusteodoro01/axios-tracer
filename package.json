{
  "name": "@gsc/axios-logger-client",
  "version": "1.0.0",
  "description": "Axios HTTP client with optional request/response logging and sensitive data removal",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "prepublish": "npm run build",
    "publish:public": "npm run build && npm publish --access public",
    "check": "npm pack --dry-run"
  },
  "keywords": [
    "axios",
    "http",
    "client",
    "logging",
    "logger",
    "request",
    "response"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^4.7.4"
  },
  "peerDependencies": {},
  "files": [
    "dist",
    "README.md"
  ]
}

